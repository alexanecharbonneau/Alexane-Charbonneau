---
// Chip pilule (fond Vert Pomme). Ex. « Bienvenue ! », « Portfolio », « Curriculum ».
interface Props { label: string; style?: string; }
const { label, style } = Astro.props;
---
<div class="chip" style={style}><span class="h2">{label}</span></div>
